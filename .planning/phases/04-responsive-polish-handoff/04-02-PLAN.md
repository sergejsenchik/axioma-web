---
phase: 04-responsive-polish-handoff
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - index.html
  - static/css/override.css
autonomous: false
requirements:
  - LANG-01
  - LANG-02
  - LANG-03
  - HAND-01
  - HAND-02
  - HAND-03
  - HAND-04

must_haves:
  truths:
    - "All visible text on the page is in Lithuanian with correct special characters"
    - "HTML lang attribute is set to lt"
    - "Every dynamic content area has a PHP marker comment"
    - "Form action URL uses a backend-ready placeholder path"
    - "All CMS-managed images use uploads/ path convention"
    - "No JavaScript errors appear in the browser console"
  artifacts:
    - path: "index.html"
      provides: "Verified Lithuanian content, PHP markers, form action path"
      contains: 'lang="lt"'
    - path: "static/css/override.css"
      provides: "Desktop styles with zero @media queries"
  key_links:
    - from: "index.html form"
      to: "PHP backend"
      via: "form action attribute"
      pattern: 'action="/submit-form.php"'
    - from: "index.html"
      to: "uploads/"
      via: "img src attributes"
      pattern: 'src="uploads/'
---

<objective>
Verify Lithuanian localization is complete and correct, ensure PHP handoff markers are comprehensive, and polish the form action convention for backend developer integration.

Purpose: Guarantee the homepage is ready for handoff to the PHP backend developer with no localization gaps, proper dynamic content markers, and compatible JavaScript.

Output: Verified and polished index.html ready for PHP CMS integration.
</objective>

<execution_context>
@/Users/sergej/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergej/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-responsive-polish-handoff/04-CONTEXT.md
@.planning/phases/04-responsive-polish-handoff/04-RESEARCH.md
@.planning/phases/04-responsive-polish-handoff/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify localization and update handoff markers</name>
  <files>index.html</files>
  <read_first>
    - index.html (full file -- scan all text content for Lithuanian correctness)
    - static/css/override.css (verify zero @media queries -- RESP-04 check)
    - static/js/script.js (verify no JS conflicts with backend stack)
    - docs/PROJECT-CONTEXT.md (backend developer's conventions for form action paths)
  </read_first>
  <action>
**Localization verification (LANG-01, LANG-02, LANG-03):**

1. Confirm `<html lang="lt">` is present on line 2 of index.html (LANG-03). It is already there -- verify, do not change.

2. Scan ALL visible text content in index.html for:
   - Correct Lithuanian special characters: ą, č, ę, ė, į, š, ų, ū, ž (and uppercase variants)
   - No English placeholder text remaining (all text should be Lithuanian)
   - No mojibake or encoding issues (file must be UTF-8)
   - Verify meta charset is `UTF-8`

3. Check these specific Lithuanian strings are correct:
   - Navbar: "Meniu", "Rinktis butą", "Registruotis į apžiūrą"
   - Hero slides: "Racionalus gyvenimas sutinka aukščiausią komfortą", "Jūsų namai, sukurti išmintingai", "Kokybė, kurią galite pajusti"
   - Hero CTA: "Gauti pasiūlymą"
   - Scroll-down: "Slinkite žemyn"
   - About heading: "Pagrindinė gyvenimo tiesa nereikalauja pagrindimo"
   - About paragraphs (verify ą,č,ė,ę,į,š,ū,ų,ž render)
   - About CTA: "Apie projektą"
   - Counter labels: "Apgalvoto dizaino rezidencijos", "Maksimalus funkcinis plotas (m2)", "Numatomas struktūros realizavimas"
   - Discover heading: "Galutinis požiūris"
   - Discover cards: h3 headings + paragraph text + "Plačiau" link
   - Location: "Matuojama minutėmis", "IKI", step words (PARKO, MIESTO CENTRO, etc.), "MINUČIŲ PĖSČIOMIS IKI ARTIMIAUSIO TAŠKO"
   - Timeline: "Statybų eiga", quarter labels, milestone descriptions
   - Contact heading: "Susisiekite -- pasikalbėsime"
   - Form placeholders: "Jūsų vardas*", "El. paštas*", "Telefonas*", "Žinutė"
   - Checkbox: "Sutinku su sąlygomis ir taisyklėmis"
   - Submit: "Siųsti"
   - Footer: "Greitos nuorodos", "Sekite mus", links (Apie, Butai, Vieta, Galerija, Kontaktai)
   - Footer: "Kiti mūsų projektai:", copyright text, "Privatumo politika"

If any text needs correction, fix it. If all text is correct, proceed.

**Handoff marker verification (HAND-01):**

4. Count PHP markers: `grep -c "PHP:" index.html` should return 28 (per research). User confirmed existing markers are sufficient -- do NOT add new ones.

5. Verify each PHP marker comment describes what the backend developer needs to replace.

**Form action update (HAND-02):**

6. Change the form action from `action="#"` to `action="/submit-form.php"` to match backend developer's convention (per research recommendation, portofranko.lt uses similar PHP controller paths). Keep the existing `<!-- PHP: form action URL -->` comment on the next line. The comment already tells the backend developer this needs their actual URL.

The specific line to change is:
```html
<!-- BEFORE: -->
<form method="post" action="#">

<!-- AFTER: -->
<form method="post" action="/submit-form.php">
```

**Image path convention (HAND-03):**

7. Verify all CMS-managed images (agent photos, contact form image) use `uploads/images/` paths. Static design assets should use `static/img/`. Check:
   - Agent photos: `uploads/images/photo-*.jpg` (5 images)
   - Contact image: `uploads/images/contact-form-image.jpg`
   - All other images: `static/img/` paths

**JS compatibility (HAND-04):**

8. Verify the JS stack has no conflicts. Check:
   - jQuery 3.7.1 (CDN) -- matches backend developer's jQuery 3.x requirement
   - jQuery UI 1.13.1 (CDN) -- matches backend developer's version exactly
   - Bootstrap 5.3.3 (CDN) -- matches backend developer's Bootstrap 5.3+
   - No fetch API calls in script.js (only $.getJSON for dev mock data, which backend replaces)
   - No ES6 modules or import/export statements
   - No async/await syntax that might fail in older browsers
   - Lucide icons CDN -- verify no conflicts

**RESP-04 verification:**

9. Run `grep -c "@media" static/css/override.css` and confirm result is 0. If any @media queries exist in override.css, they must be removed (moved to ipad.css or iphone.css as appropriate). Based on research, override.css currently has zero @media queries -- this is a verification step only.
  </action>
  <verify>
    <automated>grep -c 'lang="lt"' index.html | grep "^1$" && grep -c "PHP:" index.html | awk '{if ($1 >= 27) print "PASS: " $1 " PHP markers"; else print "FAIL: only " $1 " markers"}' && grep -c 'action="/submit-form.php"' index.html | grep "^1$" && grep -c "@media" static/css/override.css | grep "^0$" && grep -c 'src="uploads/' index.html | awk '{if ($1 >= 6) print "PASS: " $1 " uploads/ paths"; else print "FAIL: only " $1 " uploads/ paths"}'</automated>
  </verify>
  <acceptance_criteria>
    - index.html line 2 contains `lang="lt"`
    - index.html contains `<meta charset="UTF-8">`
    - index.html contains `action="/submit-form.php"` (not `action="#"`)
    - index.html contains `<!-- PHP: form action URL -->` comment near the form
    - index.html has at least 27 occurrences of `PHP:` in comments
    - index.html contains at least 6 `src="uploads/` references (5 agent photos + 1 contact image)
    - index.html contains zero English placeholder text in visible content areas
    - index.html contains Lithuanian characters: ą, č, ė, ę, į, š, ū, ų, ž (at least one of each)
    - static/css/override.css contains zero `@media` strings
    - static/js/script.js contains no `fetch(` calls (only `$.getJSON` for dev mock)
    - static/js/script.js contains no `import ` or `export ` statements
  </acceptance_criteria>
  <done>All visible text verified as Lithuanian with correct special characters. HTML lang="lt" confirmed. 28+ PHP markers in place. Form action updated to /submit-form.php. All CMS images use uploads/ convention. No JS conflicts with backend stack. No @media in override.css.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of responsive layouts</name>
  <files>index.html, static/css/ipad.css, static/css/iphone.css</files>
  <action>
Human verifies the complete responsive homepage visually across all breakpoints. Claude has automated all CSS and JS changes in Plan 01 and localization/handoff checks in Task 1 above. This checkpoint confirms the visual result matches expectations.

What was built:
- Tablet styles in ipad.css (768-1024px): adapted spacing, 2+2+1 agent grid, horizontal-scroll timeline, stacked discover cards, scaled location circle, narrower contact form
- Mobile styles in iphone.css (<=900px): single-column stacking, hamburger visible, discover overlay disabled, timeline static layout with horizontal scroll, contact image hidden, stacked agents, scaled location circle
- Lithuanian localization verified, PHP markers confirmed, form action updated to /submit-form.php
  </action>
  <verify>
    Human opens index.html in browser DevTools responsive mode and tests at 1440px, 1024px, 800px, 768px, 375px widths. Checks:
    1. At 1024px: discover cards stacked, agents 2+2+1, timeline scrollable, contact form side-by-side, location circle fits
    2. At 375px: hamburger visible, video plays, about/counter/discover/agents stacked, no purple overlay on discover tap, timeline static + horizontal scroll, contact form-only, footer stacked, all touch targets tappable
    3. At all widths: no horizontal scrollbar, Lithuanian text correct, video plays
    4. Browser console: no JavaScript errors
    5. View source: form action="/submit-form.php"
  </verify>
  <done>User approves responsive layouts at all tested breakpoints, confirms localization, confirms handoff readiness.</done>
</task>

</tasks>

<verification>
1. `grep 'lang="lt"' index.html` matches
2. `grep -c "PHP:" index.html` returns 28+
3. `grep 'action="/submit-form.php"' index.html` matches
4. `grep -c "@media" static/css/override.css` returns 0
5. `grep -c "@media" static/css/ipad.css` returns 0
6. `grep -c "@media" static/css/iphone.css` returns 0
7. No JS errors in browser console
8. Visual inspection at 375px, 800px, 1024px, 1440px widths
</verification>

<success_criteria>
- All visible text is verified Lithuanian with correct diacritics
- lang="lt" confirmed on html element
- 28+ PHP markers verified in index.html
- Form action is /submit-form.php with PHP comment
- All CMS images use uploads/ paths
- No JS conflicts with backend developer's stack
- No @media queries in override.css
- Human verification confirms responsive layouts work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-responsive-polish-handoff/04-02-SUMMARY.md`
</output>
