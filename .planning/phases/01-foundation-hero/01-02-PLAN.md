---
phase: 01-foundation-hero
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - index.html
  - static/css/override.css
  - static/js/script.js
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05

must_haves:
  truths:
    - "Opening index.html shows a full-screen video background hero that fills the viewport"
    - "Three slides auto-advance every 6 seconds with smooth crossfade transitions"
    - "Slide 1 shows centered heading text over a looping muted video"
    - "Slides 2 and 3 show left-aligned heading text over static background images"
    - "Each slide has a CTA button with the same text"
    - "A 'Slinkite zemyn' scroll indicator fades in after page load and smooth-scrolls to #about on click"
    - "Video shows poster image when autoplay is blocked"
  artifacts:
    - path: "index.html"
      provides: "Hero section with 3 slides, scroll-down indicator, #about target"
      contains: "hero-slider"
    - path: "static/css/override.css"
      provides: "Hero slider CSS, overlay, slide backgrounds, scroll-down, button styles"
      contains: "hero-section"
    - path: "static/js/script.js"
      provides: "jQuery crossfade slider logic and smooth scroll handler"
      contains: "hero-slide"
  key_links:
    - from: "static/js/script.js"
      to: "index.html .hero-slide"
      via: "jQuery selector"
      pattern: "\\$\\('.hero-slide'\\)"
    - from: "static/css/override.css"
      to: "index.html .hero-slide.active"
      via: "CSS opacity transition"
      pattern: "\\.hero-slide\\.active"
    - from: "static/js/script.js"
      to: "index.html #about"
      via: "smooth scroll on click"
      pattern: "scroll-down-link.*scrollTop"
    - from: "static/css/override.css"
      to: "static/img/hero-slide-2.jpg"
      via: "background-image url"
      pattern: "hero-slide-2\\.jpg"
---

<objective>
Build the full-screen 3-slide hero section with video background, crossfade transitions, overlay text with CTA buttons, and scroll-down indicator.

Purpose: Deliver the primary visual element of the homepage -- a working hero slider that auto-advances between 3 slides (1 video + 2 static images) with Lithuanian overlay text, matching the Webflow design's layout and typography.
Output: A fully functional hero section visible when opening index.html, with 6-second auto-advancing crossfade slider and smooth-scrolling "Slinkite zemyn" indicator.
</objective>

<execution_context>
@/Users/sergej/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergej/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-hero/01-CONTEXT.md
@.planning/phases/01-foundation-hero/01-RESEARCH.md
@.planning/phases/01-foundation-hero/01-01-SUMMARY.md

Source files to modify (AFTER Plan 01 has been executed):
@index.html
@static/css/override.css
@static/js/script.js

Reference:
@webflow-export/index.html (hero structure lines 210-331)
@CLAUDE.md

<interfaces>
<!-- CSS custom properties available from Plan 01 (in override.css :root) -->
```css
--color-primary: #0B2641;
--color-accent: #e07f60;
--color-bg: #fefaf9;
--color-white: #ffffff;
--font-body: 'Inter Tight', sans-serif;
--font-display: 'Metal', 'Inter Tight', sans-serif;
--font-size-h1: 4.5rem;
--line-height-h1: 108.33%;
--letter-spacing-h1: -0.3rem;
--font-weight-h1: 400;
--font-size-button: 0.6875rem;
--font-weight-button: 600;
--font-size-sub: 1.5rem;
```

<!-- Assets available from Plan 01 (in static/img/) -->
- static/img/hero-video.mp4
- static/img/hero-video.webm
- static/img/hero-poster.jpg
- static/img/hero-slide-2.jpg
- static/img/hero-slide-3.jpg (or .avif)
- static/img/arrow-down.webp

<!-- HTML structure after Plan 01 -->
- `<section class="hero-section" id="hero">` exists as empty placeholder
- `<section id="about">` exists as scroll target
- No navbar present (Phase 3)

<!-- From 01-RESEARCH.md -- Locked decisions -->
- Slide 1: centered heading (hero-heading-wide, max-width 40rem)
- Slides 2-3: left-aligned heading (hero-heading-wrap, max-width 30rem)
- Heading size: 4.5rem (--font-size-h1)
- Auto-advance: 6 seconds
- Crossfade transitions (not sliding)
- Dark overlay: linear-gradient(rgba(0,0,0,0.8), rgba(255,255,255,0) 30%), linear-gradient(90deg, rgba(0,0,0,0.8), rgba(255,255,255,0) 49%)
- Video: autoplay muted loop playsinline, poster fallback
- Scroll-down: "Slinkite zemyn" text with arrow, fade-in after load (no repeating bounce)
- Same CTA text on all slides: "Gauti pasiulyma" (Lithuanian for "Get a quote")

<!-- From 01-RESEARCH.md -- Button structure for future Phase 3 text-swap -->
```html
<a href="#" class="btn-cta">
  <span class="btn-text-wrap">
    <span class="btn-text btn-text-in">Gauti pasiulyma</span>
    <span class="btn-text btn-text-out">Gauti pasiulyma</span>
  </span>
</a>
```

<!-- From 01-RESEARCH.md -- Lithuanian heading translations -->
Translate Webflow English "Rational Living Meets Premium Comfort" to 3 different Lithuanian headings:
- Slide 1: centered, wide -- e.g., "Racionalus gyvenimas sutinka auksciausia komforta"
- Slide 2: left-aligned -- e.g., "Jusu namai, sukurti ismintigai"
- Slide 3: left-aligned -- e.g., "Kokybe, kuria galite pajusti"
(These are starting-point translations; client will review.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build hero HTML and CSS</name>
  <files>
    index.html
    static/css/override.css
  </files>
  <action>
    **Update index.html -- replace the empty hero-section placeholder with full hero markup:**

    The hero section structure must match the research pattern (01-RESEARCH.md Pattern 1). Inside `<section class="hero-section" id="hero">`:

    1. A `<div class="hero-slider">` containing 3 slides:

       **Slide 1 (video, centered text):**
       ```html
       <div class="hero-slide active" data-slide="1">
         <div class="hero-slide-bg">
           <video autoplay muted loop playsinline poster="static/img/hero-poster.jpg" class="hero-video">
             <source src="static/img/hero-video.mp4" type="video/mp4">
             <source src="static/img/hero-video.webm" type="video/webm">
           </video>
         </div>
         <div class="hero-overlay"></div>
         <div class="hero-content hero-content-centered">
           <div class="container">
             <div class="hero-heading-wrap hero-heading-wide">
               <h1>Racionalus gyvenimas sutinka auksciausia komforta</h1>
               <div class="hero-cta-wrap">
                 <a href="#" class="btn-cta">
                   <span class="btn-text-wrap">
                     <span class="btn-text btn-text-in">Gauti pasiulyma</span>
                     <span class="btn-text btn-text-out">Gauti pasiulyma</span>
                   </span>
                 </a>
               </div>
             </div>
           </div>
         </div>
       </div>
       ```

       **Slide 2 (static image, left-aligned text):**
       ```html
       <div class="hero-slide" data-slide="2">
         <div class="hero-slide-bg hero-slide-bg-img" style="background-image: url('static/img/hero-slide-2.jpg');"></div>
         <div class="hero-overlay"></div>
         <div class="hero-content">
           <div class="container">
             <div class="hero-heading-wrap">
               <h1>Jusu namai, sukurti ismintigai</h1>
               <div class="hero-cta-wrap">
                 <a href="#" class="btn-cta"><!-- same btn-text-wrap structure --></a>
               </div>
             </div>
           </div>
         </div>
       </div>
       ```

       **Slide 3 (static image, left-aligned text):**
       Same structure as Slide 2 with:
       - Background: `static/img/hero-slide-3.jpg` (or `.avif` if JPG conversion failed in Plan 01)
       - Heading: "Kokybe, kuria galite pajusti" (or similar Lithuanian)

    2. After the hero-slider div, the **scroll-down indicator:**
       ```html
       <div class="hero-scroll-down">
         <a href="#about" class="scroll-down-link">
           <span class="scroll-down-text">Slinkite zemyn</span>
           <span class="scroll-down-arrow">
             <img src="static/img/arrow-down.webp" alt="" width="10" height="10">
           </span>
         </a>
       </div>
       ```

    3. Ensure `<section id="about">` still exists below the hero as the scroll target (from Plan 01).

    IMPORTANT: Do NOT use any Webflow class names (w-*, rt-*). Use the clean class names documented above.
    IMPORTANT: Lithuanian text uses simplified transliterations (no special chars) as a starting point. The client will provide final Lithuanian copy.
    IMPORTANT: Build the btn-text-wrap structure on all 3 slides for future Phase 3 text-swap hover (ANIM-03). For now, only btn-text-in is visible; btn-text-out is hidden via CSS.

    **Update static/css/override.css -- add hero styles AFTER the existing container override and BEFORE the status badge styles:**

    Add these CSS blocks (values from 01-RESEARCH.md Design Tokens):

    1. **Hero section base:**
       ```css
       .hero-section {
         position: relative;
         height: 100vh;
         overflow: hidden;
         background-color: var(--color-dark);
       }
       ```

    2. **Hero slider and slides (crossfade):**
       ```css
       .hero-slider {
         position: relative;
         width: 100%;
         height: 100%;
       }
       .hero-slide {
         position: absolute;
         inset: 0;
         opacity: 0;
         transition: opacity 1s ease;
         z-index: 1;
       }
       .hero-slide.active {
         opacity: 1;
         z-index: 2;
       }
       ```

    3. **Slide backgrounds:**
       ```css
       .hero-slide-bg {
         position: absolute;
         inset: 0;
       }
       .hero-video {
         position: absolute;
         inset: 0;
         width: 100%;
         height: 100%;
         object-fit: cover;
         object-position: center;
       }
       .hero-slide-bg-img {
         background-size: cover;
         background-position: center;
         background-repeat: no-repeat;
       }
       ```

    4. **Overlay (from Webflow analysis):**
       ```css
       .hero-overlay {
         position: absolute;
         inset: 0;
         background-image: linear-gradient(rgba(0,0,0,0.8), rgba(255,255,255,0) 30%),
                           linear-gradient(90deg, rgba(0,0,0,0.8), rgba(255,255,255,0) 49%);
         z-index: 1;
       }
       ```

    5. **Content positioning:**
       ```css
       .hero-content {
         position: relative;
         z-index: 2;
         display: flex;
         align-items: flex-end;
         height: 100%;
         padding-bottom: 10rem;
       }
       .hero-content-centered {
         align-items: center;
         text-align: center;
       }
       .hero-content-centered .hero-heading-wrap {
         margin: 0 auto;
       }
       ```

    6. **Heading wraps (from Webflow widths):**
       ```css
       .hero-heading-wrap {
         max-width: 30rem;
       }
       .hero-heading-wide {
         max-width: 40rem;
       }
       .hero-section h1 {
         font-family: var(--font-display);
         font-size: var(--font-size-h1);
         line-height: var(--line-height-h1);
         letter-spacing: var(--letter-spacing-h1);
         font-weight: var(--font-weight-h1);
         color: var(--color-white);
         margin-bottom: 2rem;
       }
       ```

    7. **CTA button (from Webflow button pattern):**
       ```css
       .hero-cta-wrap {
         margin-top: 0.5rem;
       }
       .btn-cta {
         display: inline-block;
         text-transform: uppercase;
         font-family: var(--font-body);
         font-size: var(--font-size-button);
         font-weight: var(--font-weight-button);
         letter-spacing: 0.03125rem;
         line-height: 1.5rem;
         padding: 0.625rem 2.2rem;
         border: 0.0625rem solid var(--color-bg);
         background-color: var(--color-bg);
         color: var(--color-accent);
         text-decoration: none;
         transition: background-color 0.5s, color 0.5s;
       }
       .btn-cta:hover {
         background-color: transparent;
         color: var(--color-bg);
       }
       /* Button text-swap structure (visible/hidden for Phase 3 ANIM-03) */
       .btn-text-wrap {
         display: flex;
         justify-content: center;
         align-items: center;
         overflow: hidden;
         position: relative;
       }
       .btn-text {
         white-space: nowrap;
       }
       .btn-text-out {
         position: absolute;
         transform: translateY(120%);
         opacity: 0;
       }
       ```

    8. **Scroll-down indicator (from Webflow + CONTEXT.md fade-in, no bounce):**
       ```css
       .hero-scroll-down {
         z-index: 400;
         pointer-events: none;
         position: absolute;
         bottom: 0;
         left: 0;
         right: 0;
         padding-bottom: 5rem;
         opacity: 0;
         animation: fadeIn 1s ease 1.5s forwards;
       }
       .scroll-down-link {
         pointer-events: auto;
         display: flex;
         align-items: center;
         gap: 1.3125rem;
         padding-left: 0.9375rem;
         text-decoration: none;
       }
       .scroll-down-text {
         font-family: var(--font-display);
         font-size: var(--font-size-sub);
         text-transform: uppercase;
         color: var(--color-bg);
       }
       .scroll-down-arrow {
         display: flex;
         align-items: center;
       }
       @keyframes fadeIn {
         to { opacity: 1; }
       }
       ```

    CSS insertion point: AFTER the `.container` override rules, BEFORE the `/* Status badges */` comment block. This keeps the file organized: variables -> body -> container -> hero -> status.
  </action>
  <verify>
    <automated>grep -c "hero-section" index.html && grep -c "hero-slide" index.html && grep -c "Slinkite" index.html && grep -c "hero-section" static/css/override.css && grep -c "hero-slide.active" static/css/override.css && grep -c "fadeIn" static/css/override.css && echo "Hero HTML and CSS present"</automated>
  </verify>
  <done>
    - index.html has 3-slide hero section with video on slide 1, images on slides 2-3
    - Each slide has h1 heading (different Lithuanian text) and CTA button
    - Slide 1 heading is centered (hero-content-centered + hero-heading-wide)
    - Slides 2-3 headings are left-aligned (hero-heading-wrap, 30rem max)
    - Dark overlay covers all slides for text readability
    - Scroll-down indicator with "Slinkite zemyn" text and arrow image
    - All hero CSS uses custom properties from :root
    - Button has text-swap structure ready for Phase 3 animation
  </done>
</task>

<task type="auto">
  <name>Task 2: Add jQuery slider logic and smooth scroll</name>
  <files>
    static/js/script.js
  </files>
  <action>
    Add hero slider JavaScript to static/js/script.js INSIDE the existing `$(document).ready(function() { ... })` block, BEFORE the mock apartment data loading section. Add a clear comment section header.

    **Hero Slider Logic (crossfade, 6-second auto-advance):**

    ```javascript
    // -----------------------------------------------
    // Hero Slider -- Crossfade, 6s auto-advance
    // -----------------------------------------------
    var $heroSlides = $('.hero-slide');
    if ($heroSlides.length > 1) {
      var slideCount = $heroSlides.length;
      var currentSlide = 0;
      var slideInterval = 6000;
      var heroTimer;

      function goToSlide(index) {
        $heroSlides.eq(currentSlide).removeClass('active');
        currentSlide = index % slideCount;
        $heroSlides.eq(currentSlide).addClass('active');
      }

      function nextSlide() {
        goToSlide(currentSlide + 1);
      }

      function startHeroAutoplay() {
        heroTimer = setInterval(nextSlide, slideInterval);
      }

      startHeroAutoplay();
    }
    ```

    **Smooth Scroll for Scroll-Down Indicator:**

    ```javascript
    // -----------------------------------------------
    // Smooth scroll -- scroll-down indicator
    // -----------------------------------------------
    $('.scroll-down-link').on('click', function(e) {
      e.preventDefault();
      var target = $(this).attr('href');
      if ($(target).length) {
        $('html, body').animate({
          scrollTop: $(target).offset().top
        }, 800);
      }
    });
    ```

    IMPORTANT:
    - Guard the slider init with `if ($heroSlides.length > 1)` so it does nothing on pages without a hero.
    - Use `var` declarations (not `let`/`const`) to match the existing code style in script.js.
    - Keep ALL existing code (mock data loading, datepicker, fancybox, apartment table, SVG map) intact.
    - Place new code BEFORE the existing apartment data section, so the hero initializes first.
    - Do NOT add arrow/dot navigation controls (auto-advance only per discretion).
  </action>
  <verify>
    <automated>grep -c "hero-slide" static/js/script.js && grep -c "slideInterval = 6000" static/js/script.js && grep -c "scroll-down-link" static/js/script.js && grep -c "datepicker" static/js/script.js && echo "Slider JS and existing code intact"</automated>
  </verify>
  <done>
    - Hero slider auto-advances every 6 seconds via crossfade
    - Slider uses jQuery class toggling (.active) to drive CSS opacity transitions
    - Scroll-down indicator smooth-scrolls to #about section on click
    - All existing script.js functionality preserved (datepicker, fancybox, apartment table, SVG map)
    - No external slider library used -- custom jQuery only
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete hero section</name>
  <files>index.html</files>
  <action>
    User visually verifies the complete Phase 1 output in a browser. All automated work (HTML, CSS, JS) is complete at this point.

    What was built: Complete Phase 1 hero section -- full-screen video hero with 3 auto-advancing slides, Lithuanian headings, CTA buttons, dark overlay, and scroll-down indicator. Built on Bootstrap 5.3.3 foundation with correct CSS custom properties, Inter Tight + Metal fonts, and 940px container.

    How to verify:
    1. Open index.html in your browser (no server needed -- static file)
    2. Verify the hero fills the full viewport height with a dark background
    3. Slide 1: Video plays (muted, looping) with centered heading text and CTA button
    4. Wait 6 seconds -- slide transitions (crossfade) to slide 2 with a different heading (left-aligned) and static image background
    5. Wait 6 more seconds -- slide 3 appears with another heading and image
    6. After slide 3, it cycles back to slide 1
    7. Check the "Slinkite zemyn" text + arrow at the bottom -- it should fade in after ~1.5 seconds
    8. Click "Slinkite zemyn" -- page smooth-scrolls to the content placeholder below
    9. Check the CTA button -- warm off-white background, orange text, hover changes to transparent bg + light text
    10. Open DevTools > Console -- no JavaScript errors
    11. FONT CHECK: Look at the hero heading text. "Metal" font renders Latin characters but Lithuanian special characters (if any) may fall back to Inter Tight. Note if this looks acceptable or jarring.
    12. Resize browser to confirm video uses object-fit:cover (portrait video crops on wide screens -- expected behavior)

    Resume signal: Type "approved" or describe any visual/functional issues to fix.
  </action>
  <verify>
    <automated>echo "Checkpoint -- requires human visual verification in browser"</automated>
  </verify>
  <done>User confirms the hero section looks and functions correctly in the browser, or provides specific issues to address.</done>
</task>

</tasks>

<verification>
Phase 1 complete verification checklist:
1. `open index.html` -- full-screen hero visible, no console errors
2. Three slides auto-cycle with crossfade every 6 seconds
3. Slide 1: video background, centered heading, CTA
4. Slides 2-3: image backgrounds, left-aligned headings, CTAs
5. Overlay darkens all slides for text readability
6. "Slinkite zemyn" fades in and smooth-scrolls to #about
7. DevTools: `$.fn.jquery` returns "3.7.1"
8. DevTools: `.container` computed max-width is 940px
9. DevTools: `:root` has all --color-* and --font-* variables
10. `grep -r "data-wf\|data-w-\|w-slide\|w-slider" index.html` returns 0 results
11. No Webflow runtime (webflow.js) loaded
</verification>

<success_criteria>
- Opening index.html shows a premium full-screen hero slider that auto-cycles through 3 slides
- Video plays on slide 1 (muted, looping), static images on slides 2-3
- Each slide displays Lithuanian heading text in Metal/Inter Tight font at 4.5rem
- CTA buttons visible on all slides with correct styling
- Scroll-down indicator fades in and works
- All transitions are smooth without Webflow runtime dependency
- Page uses Bootstrap 5.3.3 with 940px container, all CSS custom properties from Webflow
- Clean semantic HTML -- no Webflow artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hero/01-02-SUMMARY.md`
</output>
