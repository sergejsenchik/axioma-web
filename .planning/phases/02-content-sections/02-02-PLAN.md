---
phase: 02-content-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - index.html
  - static/css/override.css
  - static/js/script.js
autonomous: true
requirements: [LOCN-01, LOCN-02, LOCN-03, LOCN-04, TIME-01, TIME-02, TIME-03, TIME-04]

must_haves:
  truths:
    - "Location section displays 'Measured in minutes' heading with a Lottie circular progress ring that animates through 5 steps"
    - "Text labels rotate in sequence showing 5 amenity destinations (park/river, city center, shopping mall, highschool, airport)"
    - "Five number circles (5, 8, 10, 15, 20) display the walking/driving times to each amenity"
    - "Timeline section shows 6 construction milestones (Q4 2024 through Q4 2026) over a construction photo background"
    - "A progress line indicates current construction position based on today's date"
  artifacts:
    - path: "index.html"
      provides: "Location and Timeline section HTML between Discover section and Contact section"
      contains: "location-section"
    - path: "static/css/override.css"
      provides: "Location circle layout, timeline grid, progress line styles"
      contains: "location-section"
    - path: "static/js/script.js"
      provides: "Lottie initialization, text rotation cycle, progress line calculation"
      contains: "lottie.loadAnimation"
  key_links:
    - from: "static/js/script.js"
      to: "static/img/circle-loader.json"
      via: "lottie.loadAnimation path parameter"
      pattern: "circle-loader\\.json"
    - from: "static/js/script.js"
      to: "index.html #circle-loading"
      via: "document.getElementById for Lottie container"
      pattern: "getElementById.*circle-loading"
    - from: "index.html"
      to: "lottie-web CDN"
      via: "script tag before script.js"
      pattern: "lottie\\.min\\.js"
---

<objective>
Build the Location ("Measured in minutes") section with Lottie circular animation and rotating text labels, and the Timeline/Construction Progress section with milestone grid and progress line.

Purpose: These two sections are the most animation-heavy in the phase -- Lottie integration for the circle progress ring and auto-cycling text rotation for Location, plus a date-calculated progress line for Timeline. They sit between Discover (built in Plan 01) and Contact (built in Plan 03).
Output: Location and Timeline sections visible and interactive in browser.
</objective>

<execution_context>
@/Users/sergej/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergej/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- CSS utilities already defined -->
From static/css/override.css (added by Plan 01):
```css
.texture-purple {
  background-color: var(--color-purple);
  background-image: url('../img/texture-purple.jpg');
  background-position: 0 0;
  background-size: 200px;
}
.divider-horizontal { width: 100%; height: 1px; background-color: var(--color-accent); }
.divider-vertical { width: 1px; align-self: stretch; background-color: var(--color-accent); }
.sub-text { font-family: var(--font-body); font-size: var(--font-size-sub); color: var(--color-accent); }
```

From static/css/override.css (existing design tokens):
```css
:root {
  --color-accent: #e07f60;
  --color-purple: #5e186e;
  --color-bg: #fefaf9;
  --color-white: #ffffff;
  --font-body: 'Inter Tight', sans-serif;
  --font-display: 'EB Garamond', 'Inter Tight', serif;
  --font-size-h2: 3.2rem;
  --font-size-h5: /* h5 not yet defined, use 1.125rem */;
  --section-gap: 8.125rem;
}
```

From index.html (after Plan 01):
```html
<!-- After Discover section, before </main> there is: -->
<!-- PHP: dynamic content sections -->
<!-- Contact and Footer sections will be added by Plan 03 -->
```

Assets already copied by Plan 01:
- static/img/circle-loader.json (Lottie animation, 30 frames)
- static/img/construction-update.jpg (timeline background)
- static/img/texture-purple.jpg (already used)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Location "Measured in minutes" section with Lottie and text rotation</name>
  <files>index.html, static/css/override.css, static/js/script.js</files>
  <action>
    **HTML (index.html):**

    1. Add lottie-web CDN script tag BEFORE the project `script.js` tag (after Lucide, before script.js):
    ```html
    <!-- Lottie (for location section animation) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lottie-web/5.13.0/lottie.min.js"></script>
    ```

    2. Add the Location section HTML after the Discover section (before the `<!-- PHP: dynamic content sections -->` comment). Structure adapted from Webflow lines 614-663:

    ```html
    <section class="location-section">
      <div class="location-container">
        <div class="location-wrapper">
          <div class="location-heading-wrap">
            <h2>Matuojama minutėmis</h2>
          </div>
          <div class="location-circle-wrapper">
            <!-- Rotating text labels -->
            <div class="location-heading-wrapper">
              <div class="location-heading-inner">
                <!-- Line 1: "IKI" (static prefix) -->
                <div class="heading-mask">
                  <h6 class="location-h6">IKI</h6>
                </div>
                <!-- Line 2: rotating second word pairs -->
                <div class="heading-mask horizontal-flex">
                  <h6 class="location-h6">&nbsp;</h6>
                  <div class="heading-mask width-applied">
                    <h6 class="location-h6 location-text-item active" data-step="0">PARKO</h6>
                    <h6 class="location-h6 location-text-item" data-step="1">MIESTO CENTRO</h6>
                    <h6 class="location-h6 location-text-item" data-step="2">ARTIMIAUSIO</h6>
                    <h6 class="location-h6 location-text-item" data-step="3">GIMNAZIJOS</h6>
                    <h6 class="location-h6 location-text-item" data-step="4">ORO UOSTO</h6>
                  </div>
                </div>
                <!-- Line 3: rotating third line -->
                <div class="heading-mask width-applied-2">
                  <h6 class="location-h6 location-text-item-2 active" data-step="0">PRIE UPĖS</h6>
                  <h6 class="location-h6 location-text-item-2" data-step="1">PĖSČIOMIS</h6>
                  <h6 class="location-h6 location-text-item-2" data-step="2">PREKYBOS CENTRO</h6>
                  <h6 class="location-h6 location-text-item-2" data-step="3">KAMPUSO</h6>
                  <h6 class="location-h6 location-text-item-2" data-step="4">AUTOMOBILIU</h6>
                </div>
              </div>
            </div>
            <!-- Number circles: 5, 8, 10, 15, 20 minutes -->
            <div class="circle-numbers-wrapper">
              <!-- PHP: walking/driving times may need verification -->
              <div class="circle-numbers-inner">
                <div class="circle-number active" data-step="0">5</div>
              </div>
              <div class="circle-numbers-inner">
                <div class="circle-number" data-step="1">8</div>
              </div>
              <div class="circle-number" data-step="2">10</div>
              <div class="circle-number" data-step="3">15</div>
              <div class="circle-number" data-step="4">20</div>
            </div>
            <!-- Static bottom text -->
            <div class="circle-text-wrapper">
              <div class="circle-text">MINUČIŲ PĖSČIOMIS IKI<br>ARTIMIAUSIO TAŠKO</div>
            </div>
            <!-- Lottie container -->
            <div id="circle-loading" class="circle-loading"></div>
          </div>
        </div>
      </div>
    </section>
    ```

    **CSS (override.css):**

    Add Location section styles after the Discover section styles block:

    ```css
    /* ----------------------------------------------- */
    /* Location / Measured in minutes                   */
    /* ----------------------------------------------- */
    ```

    Key styles:
    - `.location-section`: padding `var(--section-gap)` top/bottom, background `var(--color-bg)`, overflow hidden
    - `.location-container`: max-width for centering (do NOT use `height: 300vh` -- that is the GSAP scroll-pin pattern from Webflow. Use standard layout instead per Pitfall 5)
    - `.location-wrapper`: position relative, display flex, flex-direction column, align-items center
    - `.location-heading-wrap h2`: EB Garamond italic, centered, margin-bottom
    - `.location-circle-wrapper`: position relative, width `36em` (576px per Webflow), aspect-ratio 1/1, border-radius 50%, border `1px solid #e07f604a` (accent at 29% opacity), margin 0 auto, display flex, align-items center, justify-content center
    - `.location-heading-wrapper`: position absolute, text-align center, z-index 2
    - `.location-heading-inner`: display flex, flex-direction column, align-items center
    - `.heading-mask`: overflow hidden
    - `.heading-mask.horizontal-flex`: display flex, align-items baseline
    - `.heading-mask.width-applied, .heading-mask.width-applied-2`: position relative, overflow hidden, height matching one h6 line
    - `.location-h6`: font-family var(--font-body), font-size h5/h6 scale, text-transform uppercase, letter-spacing, font-weight 500
    - `.location-text-item, .location-text-item-2`: position absolute, opacity 0, transform translateY(100%), transition opacity 0.4s, transform 0.4s
    - `.location-text-item.active, .location-text-item-2.active`: opacity 1, transform translateY(0)
    - `.circle-numbers-wrapper`: position absolute, inset 0, z-index 1
    - `.circle-number`: position absolute, font-size 140px (per Webflow), font-family var(--font-body), font-weight 500, opacity 0, transform scale(0.8), transition opacity 0.4s, transform 0.4s
    - `.circle-number.active`: opacity 1, transform scale(1)
    - Position each `.circle-number` around the circle using absolute positioning (top/left/right/bottom percentages matching Webflow layout -- the 5 numbers are distributed around the circle perimeter)
    - `.circle-text-wrapper`: position absolute, bottom area of circle, text-align center
    - `.circle-text`: font-family var(--font-body), font-size small, text-transform uppercase, letter-spacing, color muted
    - `#circle-loading.circle-loading`: position absolute, inset 0, width 101%, height 101% (per Pitfall 2 from research -- Lottie SVG sizing)
    - `.circle-loading svg`: width 100%, height 100%

    **JavaScript (script.js):**

    Add inside `$(document).ready()` after the counter observer code:

    ```javascript
    // -----------------------------------------------
    // Location -- Lottie circle + text rotation
    // -----------------------------------------------
    var $locationSection = $('.location-section');
    if ($locationSection.length && typeof lottie !== 'undefined') {
      var circleAnim = lottie.loadAnimation({
        container: document.getElementById('circle-loading'),
        renderer: 'svg',
        loop: false,
        autoplay: false,
        path: 'static/img/circle-loader.json'
      });

      var locationSteps = [
        { frame: 6 },   // Step 0: Park / By the river (5 min)
        { frame: 12 },  // Step 1: City center / On foot (8 min)
        { frame: 18 },  // Step 2: Shopping mall (10 min)
        { frame: 24 },  // Step 3: Highschool campus (15 min)
        { frame: 30 }   // Step 4: Airport / With car (20 min)
      ];

      var currentLocationStep = 0;
      var locationCycleTimer = null;

      function advanceLocationStep() {
        // Remove active from all
        $('.location-text-item, .location-text-item-2, .circle-number').removeClass('active');

        // Set current step active
        var step = locationSteps[currentLocationStep];
        $('.location-text-item[data-step="' + currentLocationStep + '"]').addClass('active');
        $('.location-text-item-2[data-step="' + currentLocationStep + '"]').addClass('active');
        $('.circle-number[data-step="' + currentLocationStep + '"]').addClass('active');

        // Play Lottie from previous frame to current
        var fromFrame = currentLocationStep === 0 ? 0 : locationSteps[currentLocationStep - 1].frame;
        circleAnim.playSegments([fromFrame, step.frame], true);

        currentLocationStep = (currentLocationStep + 1) % locationSteps.length;
      }

      function startLocationCycle() {
        advanceLocationStep(); // Show first step immediately
        locationCycleTimer = setInterval(advanceLocationStep, 3500); // 3.5s per step
      }

      // Trigger on scroll into viewport
      var locationObserver = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
          if (entry.isIntersecting) {
            startLocationCycle();
            locationObserver.unobserve(entry.target);
          }
        });
      }, { threshold: 0.3 });

      locationObserver.observe($locationSection[0]);
    }
    ```

    IMPORTANT: Do NOT replicate Webflow's 300vh height + sticky positioning. Use a standard section height with auto-cycling timer triggered by IntersectionObserver (per research recommendation and Pitfall 5). The Lottie animation plays 6-frame segments synced with each text rotation step.
  </action>
  <verify>
    <automated>grep -c "location-section\|circle-loading\|circle-number" index.html | xargs -I{} test {} -ge 3 && grep -c "location-section\|circle-number\|circle-loading" static/css/override.css | xargs -I{} test {} -ge 3 && grep -c "lottie.loadAnimation\|locationObserver\|advanceLocationStep" static/js/script.js | xargs -I{} test {} -ge 3 && grep -c "lottie.min.js" index.html | xargs -I{} test {} -ge 1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Location section displays "Matuojama minutėmis" heading centered above a circular layout
    - Lottie circular progress ring animates inside the circle when section enters viewport
    - Text labels rotate through 5 amenity destinations every 3.5 seconds
    - Five number values (5, 8, 10, 15, 20) appear/disappear in sync with text rotation
    - Circle has thin accent-colored border (1px solid at 29% opacity)
    - lottie-web 5.13.0 CDN script tag present in index.html before script.js
    - No 300vh height, no sticky positioning, no GSAP
    - PHP markers on walking/driving time values
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Timeline/Construction Progress section with milestone grid and progress line</name>
  <files>index.html, static/css/override.css, static/js/script.js</files>
  <action>
    **HTML (index.html):**

    Add the Timeline section after the Location section. Structure adapted from Webflow lines 664-722:

    ```html
    <section class="timeline-section">
      <div class="container-wide">
        <div class="timeline-wrapper">
          <div class="timeline-main">
            <!-- Construction photo background -->
            <div class="timeline-background-image">
              <img src="static/img/construction-update.jpg" alt="" class="timeline-image">
              <div class="timeline-image-overlay"></div>
            </div>
            <!-- Timeline content overlay with purple texture -->
            <div class="timeline-content texture-purple">
              <div class="timeline-progress-wrap">
                <div class="timeline-progress-header">
                  <h2>Statybų<br>eiga</h2>
                </div>
                <!-- Progress line: animated width based on current date -->
                <!-- PHP: construction progress percentage -->
                <div class="progress-line">
                  <div class="progress-line-fill"></div>
                </div>
                <!-- 6-column milestone grid -->
                <div class="timeline-grid">
                  <!-- PHP: milestone status -->
                  <div class="timeline-card">
                    <div class="timeline-card-content">
                      <div class="timeline-quarter">2024 Q4</div>
                      <p>Statybų pradžia</p>
                    </div>
                  </div>
                  <div class="timeline-card">
                    <div class="timeline-card-content">
                      <div class="timeline-quarter">2025 Q3</div>
                      <p>Požeminės automobilių aikštelės konstrukcijos</p>
                    </div>
                  </div>
                  <div class="timeline-card">
                    <div class="timeline-card-content">
                      <div class="timeline-quarter">2026 Q1</div>
                      <p>Antžeminės pastato dalies konstrukcijos</p>
                    </div>
                  </div>
                  <div class="timeline-card">
                    <div class="timeline-card-content">
                      <div class="timeline-quarter">2026 Q2</div>
                      <p>Stogas</p>
                    </div>
                  </div>
                  <div class="timeline-card">
                    <div class="timeline-card-content">
                      <div class="timeline-quarter">2026 Q3</div>
                      <p>Fasadas</p>
                    </div>
                  </div>
                  <div class="timeline-card">
                    <div class="timeline-card-content">
                      <div class="timeline-quarter">2026 Q4</div>
                      <p>Statybų pabaiga</p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
    ```

    Milestone text is already in Lithuanian from the Webflow export (confirmed real data per user decision).

    **CSS (override.css):**

    Add Timeline section styles after Location styles:

    ```css
    /* ----------------------------------------------- */
    /* Timeline / Construction progress                 */
    /* ----------------------------------------------- */
    ```

    Key styles:
    - `.timeline-section`: padding `var(--section-gap)` top/bottom
    - `.timeline-wrapper`: position relative
    - `.timeline-main`: position relative, overflow hidden
    - `.timeline-background-image`: position relative
    - `.timeline-image`: width 100%, display block, object-fit cover
    - `.timeline-image-overlay`: position absolute, inset 0 (semi-transparent dark overlay to improve text readability over photo -- optional, check Webflow)
    - `.timeline-content.texture-purple`: position relative (NOT absolute overlay on photo -- Webflow uses a flex layout where the photo is above and the timeline overlay is below/on top. Check Webflow CSS carefully. The Webflow structure shows photo as background image, then the timeline content as an overlay)
    - `.timeline-progress-header h2`: EB Garamond italic, color white
    - `.progress-line`: width 100%, height 1.5px, background rgba(255,255,255,0.3), position relative, margin 1.5rem 0
    - `.progress-line-fill`: position absolute, top 0, left 0, height 100%, background var(--color-white), transition width 1s ease
    - `.timeline-grid`: display grid, `grid-template-columns: repeat(6, 1fr)`, border-top `1px solid rgba(255,255,255,0.2)`
    - `.timeline-card`: border-top `1px solid rgba(255,255,255,0.2)`, padding-top 1rem
    - `.timeline-quarter`: font-family var(--font-body), font-weight 500, color white, margin-bottom 0.5rem (h5-style per Webflow's `.rt-text-style-h5`)
    - `.timeline-card p`: color rgba(255,255,255,0.7), font-size body

    **JavaScript (script.js):**

    Add timeline progress calculation after location code:

    ```javascript
    // -----------------------------------------------
    // Timeline -- Progress line based on current date
    // -----------------------------------------------
    var $progressFill = $('.progress-line-fill');
    if ($progressFill.length) {
      var startDate = new Date(2024, 9, 1);  // Oct 2024 (Q4 2024 start)
      var endDate = new Date(2026, 11, 31);   // Dec 2026 (Q4 2026 end)
      var now = new Date();
      var progress = Math.min(100, Math.max(0,
        ((now - startDate) / (endDate - startDate)) * 100
      ));
      $progressFill.css('width', progress + '%');
    }
    ```

    This calculates progress as percentage of elapsed time from project start (Oct 2024) to end (Dec 2026). As of March 2026, this should show roughly 65% progress.
  </action>
  <verify>
    <automated>grep -c "timeline-section\|timeline-grid\|timeline-card\|progress-line" index.html | xargs -I{} test {} -ge 4 && grep -c "timeline-section\|timeline-grid\|progress-line" static/css/override.css | xargs -I{} test {} -ge 3 && grep -c "progress-line-fill\|startDate\|endDate" static/js/script.js | xargs -I{} test {} -ge 2 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Timeline section displays 6 milestones in a 6-column CSS grid (Q4 2024 through Q4 2026)
    - Each milestone shows quarter label and Lithuanian description
    - Construction photo visible as background
    - Purple texture overlay on timeline content area
    - White progress line shows current construction position (~65% width as of March 2026)
    - Progress line width is calculated from current date, not hardcoded
    - "Statybų eiga" heading in EB Garamond italic, white
    - PHP markers present for construction progress percentage and milestone status
    - All milestone text matches Webflow export Lithuanian content exactly
  </done>
</task>

</tasks>

<verification>
1. Open index.html in browser
2. Scroll to Location section -- circle should appear with text "Matuojama minutėmis"
3. Watch text rotate through 5 destinations with Lottie circle animation
4. Verify number circles (5, 8, 10, 15, 20) cycle in sync
5. Scroll to Timeline -- construction photo with purple overlay should be visible
6. 6 milestone cards should display in a grid
7. Progress line should show approximately 65% filled
8. Check browser console for any JS errors (especially Lottie loading)
</verification>

<success_criteria>
- Location Lottie animation plays correctly when section enters viewport
- Text labels and numbers cycle through 5 steps automatically
- No console errors from Lottie loading
- Timeline grid renders 6 milestones in correct order
- Progress line width matches current date calculation
- Purple texture visible on timeline overlay
- No 300vh empty space (Webflow scroll-pin removed)
- All text in Lithuanian
- PHP markers present
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-02-SUMMARY.md`
</output>
