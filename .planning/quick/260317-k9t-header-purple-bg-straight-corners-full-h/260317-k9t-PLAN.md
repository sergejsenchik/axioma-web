---
phase: quick
plan: 260317-k9t
type: execute
wave: 1
depends_on: []
files_modified:
  - static/css/override.css
  - static/css/iphone.css
  - static/css/ipad.css
autonomous: true
requirements: [header-purple-bg-refinement]
must_haves:
  truths:
    - "Purple logo area has straight rectangular corners (no rounding)"
    - "Purple background spans full header height, flush to browser top"
    - "On scroll, purple expands horizontally from logo width to full header width with smooth animation"
    - "Navbar content (logo, links, menu) remains above the purple background"
  artifacts:
    - path: "static/css/override.css"
      provides: "::before pseudo-element expanding mask on .site-navbar"
      contains: ".site-navbar::before"
    - path: "static/css/iphone.css"
      provides: "Mobile responsive navbar styles updated for pseudo-element approach"
    - path: "static/css/ipad.css"
      provides: "Tablet responsive navbar styles updated for pseudo-element approach"
  key_links:
    - from: ".site-navbar::before"
      to: ".navbar-scrolled"
      via: "width transition from ~10rem to 100%"
      pattern: "site-navbar::before.*width"
---

<objective>
Refine the header purple background: remove rounded corners, make it full-height flush to top, and implement a smooth expanding mask animation using a ::before pseudo-element that grows from logo width to full header width on scroll.

Purpose: Polish the header scroll animation from an abrupt background swap to a smooth horizontal expansion.
Output: Updated CSS across override.css, iphone.css, ipad.css
</objective>

<execution_context>
@/Users/sergej/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergej/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@static/css/override.css (lines 395-469 — navbar section)
@static/css/iphone.css (lines 25-44 — navbar overrides)
@static/css/ipad.css (lines 27-35 — navbar overrides)
@static/js/script.js (lines 128-141 — scroll handler, no JS changes needed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace navbar background approach with ::before expanding mask</name>
  <files>static/css/override.css, static/css/iphone.css, static/css/ipad.css</files>
  <action>
In override.css, refactor the navbar section (lines ~399-449):

1. **`.site-navbar`** — Remove `transition: background-color 0.4s ease, background-image 0.4s ease` (background is now handled by pseudo-element). Keep position/z-index as-is.

2. **`.site-navbar.navbar-scrolled`** — Remove the entire rule block (lines 409-414). The full-width purple is now handled by the ::before expanding to 100%.

3. **Add `.site-navbar::before`** — New rule:
   ```css
   .site-navbar::before {
     content: '';
     position: absolute;
     top: 0;
     bottom: 0;
     left: 50%;
     transform: translateX(-50%);
     width: 10rem;
     background-color: var(--color-purple);
     background-image: url('../img/texture-purple.jpg');
     background-position: 0 0;
     background-size: 200px;
     transition: width 0.4s ease;
     z-index: 0;
   }
   ```
   The width of `10rem` matches the logo area (~8rem logo + padding). Centered via left:50% + translateX(-50%).

4. **`.site-navbar.navbar-scrolled::before`** — New rule:
   ```css
   .site-navbar.navbar-scrolled::before {
     width: 100%;
   }
   ```

5. **`.navbar-container`** — Add `position: relative; z-index: 1;` so all navbar content (logo, links, hamburger) sits above the ::before pseudo-element.

6. **`.navbar-center`** — Remove ALL background properties (background-color, background-image, background-position, background-size). Remove `border-radius: 0 0 1rem 1rem`. Remove `transition: background-color 0.4s ease, padding 0.4s ease, border-radius 0.4s ease`. Keep: `width: auto; min-width: 5rem; display: flex; justify-content: center; padding: 0.5rem 1.25rem;`

7. **`.site-navbar.navbar-scrolled .navbar-center`** — Remove the entire rule block (lines 444-449). No longer needed since purple bg is handled by ::before.

In iphone.css:
- **`.site-navbar.navbar-scrolled .navbar-center`** — Remove this entire rule block (lines 36-40). No longer relevant.
- **`.navbar-center`** — Keep the existing overrides (width: auto, flex: 1, justify-content: flex-start) but ensure no background or border-radius properties remain.

In ipad.css:
- **`.site-navbar.navbar-scrolled .navbar-center`** — Remove this entire rule block (lines 31-35). No longer relevant.
  </action>
  <verify>
    <automated>grep -n "site-navbar::before" static/css/override.css && grep -n "border-radius" static/css/override.css | grep -v "navbar" || echo "No border-radius on navbar - PASS" && echo "---" && grep -c "navbar-scrolled .navbar-center" static/css/iphone.css static/css/ipad.css | grep ":0" && echo "Scrolled center rules removed - PASS"</automated>
  </verify>
  <done>
    - .site-navbar::before exists with purple textured bg, width: 10rem, centered, transition: width 0.4s ease
    - .site-navbar.navbar-scrolled::before sets width: 100%
    - .navbar-container has position: relative and z-index: 1
    - .navbar-center has NO background, NO border-radius
    - .site-navbar.navbar-scrolled has NO background (removed)
    - .site-navbar.navbar-scrolled .navbar-center rules removed from all 3 CSS files
    - Purple background is flush to top (::before uses top: 0)
    - Corners are straight (no border-radius anywhere on navbar purple)
    - On scroll, purple smoothly expands from logo width to full width
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Header purple background refactored: straight corners, full height flush to top, smooth expanding mask animation from logo width to full bar on scroll via ::before pseudo-element.</what-built>
  <how-to-verify>
    1. Open index.html in browser
    2. At top of page (no scroll): purple background should be a narrow rectangle behind the logo, straight corners (no rounding), flush to the very top of the browser window
    3. Scroll down past ~50px: purple should smoothly expand horizontally from the logo area to fill the entire header width
    4. Scroll back to top: purple should smoothly contract back to logo width
    5. Verify navbar links and logo remain visible and clickable throughout the animation
    6. Check mobile viewport (<=900px): purple expansion should work the same way
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Purple bg has no border-radius anywhere (straight rectangular corners)
- Purple bg is flush to browser top edge (position: absolute, top: 0)
- Smooth horizontal expansion animation on scroll (width transition 0.4s)
- All navbar content sits above purple bg (z-index layering)
- No leftover background properties on .navbar-center or .site-navbar.navbar-scrolled
</verification>

<success_criteria>
- Header purple background is a straight rectangle, full header height, flush to top
- On scroll, purple smoothly expands from ~10rem centered width to 100% width
- On scroll back to top, purple smoothly contracts back
- Navbar content remains interactive throughout
- Responsive files cleaned up (no orphaned scrolled rules)
</success_criteria>

<output>
After completion, create `.planning/quick/260317-k9t-header-purple-bg-straight-corners-full-h/260317-k9t-SUMMARY.md`
</output>
